# Secret ids the workflow reads with runtime.getSecret({ id }) -> environment variables that hold the values
# (from .env for simulation; from the Vault DON for deployed workflows via `cre secrets create`).
secretsNames:
  LLM_API_KEY:
    - LLM_API_KEY_VALUE
