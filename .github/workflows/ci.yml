name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scaffold only
        run: echo "CI jobs land in Phase 9 (forge test, sdk build, secret scan)."
